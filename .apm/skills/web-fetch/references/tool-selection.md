# Fetch Tool Selection

- Use product-specific CLIs for product-specific resources.
- Use official documentation for API and SDK questions.
- Use a general web fetch only when no direct structured source exists.
- If a site is JS-heavy or blocks simple fetches, escalate to the richer fetch path.

