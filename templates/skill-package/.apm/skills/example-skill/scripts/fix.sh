#!/usr/bin/env bash
# Nested under the skill dir alongside check.sh -- see check.sh for why.
set -euo pipefail

# Replace with the auto-fixable subset (formatting, codegen, etc.).
echo "example-skill: applying auto-fixes"
