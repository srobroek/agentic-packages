# Python Library Defaults

- Prefer FastAPI with Pydantic for APIs.
- Use Litestar when the project needs stronger application structure.

Keep existing project choices unless the task is explicitly about setup,
migration, or standardization.
