agent: maintenance-metrics-reader
regime: clean
prompt: |
  Collect maintenance health signals for this repository.
  scope: full repository
sandbox:
  git: true
  files:
    README.md: |
      # Test repo
assert:
  first_line: '^MAINTENANCE SNAPSHOT \S+ status=(?:PASS|WARN|FAIL)'
  max_words: 60
  no_reprint: true
  required_patterns:
    - 'status=PASS'
timeout_s: 180
budget_usd: 0.50
