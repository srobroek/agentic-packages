skips:
  # orchestrate fleet (bead-as-brief v2 — requires live run state)
  - agent: domain-specialist
    reason: >-
      Requires live orchestrate run state (claimed bead, BRIEF comment, pushable
      remote worktree, bd DB with epic); faithful stub deferred — tracked in spec 002.
  - agent: domain-specialist-high
    reason: Same as domain-specialist (effort variant); deferred with the base definition.
  - agent: domain-specialist-medium
    reason: Same as domain-specialist (effort variant); deferred with the base definition.
  - agent: domain-specialist-xhigh
    reason: Same as domain-specialist (effort variant); deferred with the base definition.
  - agent: reviewer
    reason: >-
      Requires live orchestrate run state (review wisp to claim, node bead with
      pushed branch, bd + gh); deferred — tracked in spec 002.
  - agent: advisor
    reason: >-
      Requires live escalation wisp to claim + answer; deferred — tracked in spec 002.
  - agent: researcher
    reason: >-
      Requires live orchestrate run state (claimed bead, artifacts_dir, output_ref);
      deferred — tracked in spec 002.
  - agent: scribe
    reason: >-
      Requires live bd database with populated run epic and ledger wisps; deferred —
      tracked in spec 002.
  - agent: shepherd
    reason: >-
      Requires live merge bead queue, merge slot, remote repository with PRs, and
      sheepdog wisp lease; deferred — tracked in spec 002.
