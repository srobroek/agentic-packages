# TypeScript Tooling Defaults

- Use Bun where no legacy package-manager constraint exists.
- Use pnpm for existing monorepos standardized on it.
- Use Vitest for tests unless the existing stack already standardizes on a
  different runner.
