{
  "mcpServers": {
    "serena": {
      "command": "bash",
      "args": [
        "-lc",
        "launcher_rel=\"apm_modules/srobroek/agentic-packages/packages/mcp-serena/scripts/serena-pool.py\"; for apm_root in \"${APM_HOME:-}\" \"${HOME}/.apm\" \"$PWD/.apm\"; do [ -n \"$apm_root\" ] || continue; launcher=\"$apm_root/$launcher_rel\"; [ -r \"$launcher\" ] && exec python3 \"$launcher\"; done; echo serena-apm-launcher-not-found-run-apm-install >&2; exit 1"
      ]
    }
  }
}
