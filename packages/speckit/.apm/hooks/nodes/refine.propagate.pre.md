# /speckit.refine.propagate — before you run this

## Came from
- /speckit.refine.diff

## Preconditions
- HARD-MISSING: specs/<feat>/refine-diff.md

## Context absorbed from steering
- Mechanically pushes the diff into downstream artefacts (plan, tasks, issues). Don't accept blindly — review propagated edits.
