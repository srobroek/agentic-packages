# /speckit.cleanup — what to do next

## Going to
- /speckit.memory-md.capture (default — capture lessons while context is fresh)
- /speckit.sync.analyze (if no lessons to capture)

## Postconditions
- `specs/<feat>/cleanup-report.md`
