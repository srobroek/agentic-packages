# /speckit.worktree.create — what to do next

## Going to
- /speckit.worktree.list (advisory — confirm the worktree was created)

## Postconditions
- new git worktree under the configured worktree root
