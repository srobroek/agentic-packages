# /speckit.security-review — before you run this

## Came from
- /speckit.qa.run (default)
- /speckit.review.run (acceptable if qa skipped)

## Preconditions
- SOFT: implementation diff present

## Context absorbed from steering
- Dedicated security audit of pending changes on the current branch. Run the /security-review skill as a subagent. Can run in parallel with /speckit.code-review.
