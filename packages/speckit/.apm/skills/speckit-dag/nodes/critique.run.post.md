# /speckit.critique.run — what to do next

## Going to
- /speckit.diagram.dependencies (default, after security-review also completes)
- /speckit.refine.update (if critique reveals plan-level rework)

## Postconditions
- `specs/<feat>/critique-report.md`
