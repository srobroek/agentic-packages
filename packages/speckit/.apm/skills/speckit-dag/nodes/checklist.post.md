# /speckit.checklist — what to do next

## Going to
- /speckit.memory-md.plan-with-memory (preferred when memory-md is installed)
- /speckit.plan (otherwise)

## Postconditions
- `specs/<feat>/checklist.md`

## Conditional branching
- If memory-md is installed but `docs/memory/INDEX.md` is missing, run `/speckit.memory-md.init` first.
