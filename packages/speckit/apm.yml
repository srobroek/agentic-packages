name: speckit
version: 0.1.0 # x-release-please-version
description: "SpecKit mechanism: the per-command DAG dispatcher skill (speckit-dag) and its ~173 node markdowns, the six SpecKit agents, and the bugfix and setup skills. Opt into the opinionated layers separately: steering-speckit (the mandatory-gated workflow) and speckit-dag-hooks (the enforcement hooks)."
type: hybrid
target: all
includes: auto
