name: speckit
version: 0.1.0 # x-release-please-version
description: "SpecKit mechanism: per-command DAG hook dispatcher, the six SpecKit agents, the bugfix and setup skills, and hooks. The opinionated mandatory-gated workflow lives in the separate steering-speckit package — install it to adopt that process."
type: hybrid
target: all
includes: auto
