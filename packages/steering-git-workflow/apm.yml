name: steering-git-workflow
version: 2.3.4
description: >-
  Git workflow policy: branch discipline, ship-path choice (merge vs PR), merge
  flags, draft-first PRs, Beads body linkage, and pre-push verification. Ships a
  dual-runtime PreToolUse blocker for non-draft or unlinked PR creation.
  Replaces the retired commit-push-* skills and git nudge hooks.
author: Sjors Robroek
license: Apache-2.0
type: hybrid
target: all
includes: auto
category: steering
tags:
  - steering
  - git
